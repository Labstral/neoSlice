# UI
PySide6>=6.11.0
pyvista>=0.48.0
pyvistaqt>=0.11.4

# Géométrie 3D
trimesh>=4.12.0
numpy>=2.4.0
scipy>=1.17.0
networkx>=3.0          # requis par trimesh (composants connexes, split multi-corps)
lxml>=5.0              # requis par trimesh (chargement XML des fichiers 3MF)

# Diagnostic IA (Pro) — inférence ONNX locale
onnxruntime>=1.18.0

# neoGen (Pro) — génération de pièces 3D par texte (NE JAMAIS retirer :
# sans ces libs, "No module named ..." au runtime comme networkx en v0.1.5.4)
matplotlib>=3.10           # contours vectoriels des polices (TextPath)
mapbox_earcut>=1.0         # triangulation des polygones (trimesh extrude)
manifold3d>=3.0            # unions/différences booléennes étanches
segno>=1.6                 # QR code 3D (matrice hors-ligne, pur Python, sans dépendance)
svgelements>=1.9           # import de logos SVG
opencv-python-headless>=4.10   # vectorisation de logos PNG (contours)

# Certificats SSL (HTTPS fiable en app gelée, surtout macOS)
certifi>=2024.0.0

# Data models
pydantic>=2.13.0

# Utilitaires
PyYAML>=6.0.3
loguru>=0.7.3
rtree>=1.4.0
shapely>=2.0.0
reportlab>=4.0.0
psutil>=6.0            # RAM/config machine sur macOS/Linux (settings — analyse de configuration)
pyinstaller>=6.20.0

# Tests (dev uniquement, non embarqué dans le build)
pytest>=8.0
