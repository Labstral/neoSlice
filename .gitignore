# Environnement virtuel (tous les venv, ex: .venv, .venv312…)
.venv/
.venv*/
venv/

# Build PyInstaller
build/
dist/
*.spec.bak

# Caches / artefacts générés
.token-savior-cache.json
graphify-out/
models/
recovered/

# Base de connaissances Oen (~5 Go : wikis moissonnés + index RAG) — JAMAIS dans
# git (vectors.npy = 1,5 Go > limite GitHub 100 Mo, un push planterait). Ces
# fichiers vivent dans la release GitHub `neoslice-assets` (voir kb_update.py).
data/kb/

# Fichiers sensibles (NE JAMAIS committer)
Backup codes Lemon Squeezy.txt
*secret*
*token*
**/.kaggle/
*USERPROFILE/

# Cache Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Logs
*.log

# Fichiers système
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
release.sh
*.env
nodex.exe
test-mcp*.js
test-mcp*.bat
context7-mcp.bat

# Config MCP locale (outils dev, non publiée)
.mcp.json

# Artefacts de calibration / scraping (dev)
_calib/
_orca_help.txt
*.gcode

# Log de build transitoire
build_log.txt

# Artefacts de test des bots slicer (sortie ffslicer/CLI)
result.json
