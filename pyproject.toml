[project]
name = "neuroprint"
version = "0.1.0"
description = "AI-powered 3D printing intent translator for Bambu Studio"
requires-python = ">=3.11"

[project.scripts]
neuroprint = "main:main"

[tool.ruff]
line-length = 100
target-version = "py311"
